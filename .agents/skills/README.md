# Local Agent Skills

This directory is reserved for local or authoring-only skills used by the development environment.

Published MAF skills belong under `plugins/<plugin>/skills/<skill>/` and are advertised by the marketplace manifests under `.agents/plugins/`.

Do not duplicate a published skill here. Keep one source of truth and use the plugin package for distributable skills.
